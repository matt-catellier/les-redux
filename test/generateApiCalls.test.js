var assert  = require('assert');

describe('Given output folder has contents in it', () => {
  describe('When I call clearOutput', () => {
    it('Then output folder should have no files in it', () => {
      assert.equal(1, 0)
    })
  })
});